version: "3.9"

services:
  api:
    build: .
    container_name: ai-risk-manager-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./audit:/app/audit      # persist audit logs
      - ./artifacts:/app/artifacts
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile.dashboard
    container_name: ai-risk-manager-dashboard
    ports:
      - "8501:8501"
    env_file:
      - .env
    depends_on:
      - api
    restart: unless-stopped